using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace R2
{
    public enum AttackInputs {
        rb, lb, rt, lt, none
    }

    public enum InteractionType {
        pickup, talk, open, cathedral, statue, read
    }

}
